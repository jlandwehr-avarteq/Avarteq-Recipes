<!-- Fixed navbar -->
<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <%= link_to "Avarteq Recipes", root_path, class: "navbar-brand" %>
          </div>
          <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
              <li class="<%= is_active?(root_path) %>"> <%= link_to t('header.home'), root_path %></li>
              <%= render 'devise/menu/friends_item' %>
              <li> <%= t('header.recipes') %> </li>
              <li><%= t('header.groups') %> </li>
              <li><%= t('header.my_recipes') %> </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
              <%= render 'devise/menu/allergies' %>
              <%= render 'devise/menu/ingredients' %>
              <%= render 'devise/menu/user_profile' %>
              <%= render 'devise/menu/login_items' %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </nav>
    </div>
  </div>
</div>
